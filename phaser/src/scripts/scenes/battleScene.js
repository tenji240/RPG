export default class BattleScene extends Phaser.Scene {
  constructor() {
    super({key: 'BattleScene'});
  }

  preload() {
    // ** load images here
  }

  create() {
    // ** create the level here
  }

  update() {
    // ** read button clicks and act accordingly
  }
}